export default function markdownParser() {

    //loop through string build array of types
    //split string using regex encompassing all flags
  return (
    `Array with pairs of entries e.g.:
    [[type][content], [type],[content]]`
  )
}
